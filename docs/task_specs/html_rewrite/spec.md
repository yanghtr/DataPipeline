任务背景: 我们要实现一个真实网站 HTML 改写数据生产 pipeline。输入是一个 JSONL 文件 @../Data/raw/FineWebEdu/samples10.jsonl  ，每条数据包含原始网页 HTML（字段为 `html`，可能还包含 url、id、page_type 等元信息，最后也要记录在合成数据的meta信息里面）。流程是：先读取原始 HTML，按照 @docs/task_specs/html_rewrite/minimal_html_preprocess_spec_media.md  做最小预处理；然后参考现有蒸馏项目 @distillation/  的 README 和工程结构，复用其中 API 调用、并发、retry、resume、日志和结果保存的设计，调用 OpenAI-compatible 模型接口，将预处理后的 dirty HTML 改写成干净、单文件、可渲染的 HTML (注意结果要存一些原始的meta信息，比如url等，方便我们后面检查回溯)。预处理不要做结构重写，只做必要的资源路径替换、超长内容截断和格式标准化；真正的页面重写交给模型完成。 请优先保持工程模块化，帮我在文件夹 @html_rewrite 里面设计一个模块化的代码管线，先不写代码，先做整体设计，总体设计文档写在 @docs/reports/html_rewrite