帮我写一个可视化网站对比实验结果。背景如下：每一个实验的结果都存在各自的某一个目录。对于每一个目录有如下内容：f"gt_{index}.svg", f"gt_{index}.png", f"gen_{index}.svg", f"gen_{index}.png"，其中index都是一一对应的，.png 是对应的 .svg文件渲染结果。对于每一个实验， f"gt_{index}.svg", f"gt_{index}.png" 都是一样的，所以只要选第一个目录的作为参考来可视化就行。现在希望如下功能：指定输入的实验结果目录，有多列做对比，其中最左边一列给出GT，然后第二、三...依次是实验的结果（即gen）。然后对于每一列，我希望从上到下同时展示出：对应渲染的图片(gt/gen_*.png), svg代码（gt/gen_*.svg），以及在线实时渲染的SVG（注意确保每一列的svg源码都会被渲染）

补充需求:
现在每一个实验结果的目录还有一个`details.jsonl`文件，里面记录了每一条生成结果的分数，其中按顺序第i条（从0开始计数）对应`f"gt/gen_{i}.svg/png"`里面的`i`。这个文件对于每一条数据（针对预测）都记录了相关的评测信息和分数，有的字段包括：status (可以取值 "success"，其他都是失败的如"render_fail", "nan"等)，还有其他字段:ssim/lpips/clip_score/low-level/isvgen_score这些scalar分数。现在我想在已有可视化基础上，对于每一条预测的数据，都补上这些评测的相关信息。然后最重要是我可以根据scalar分数进行排序，排序时会指定到底是哪个实验的分数进行排序，然后其它实验相同index的数据会同步跟着这个排序，即保证每一条数据都是对应可以比较的。实现代码并给出使用说明